import { NgDropImagesDirective } from './ng-drop-images.directive';

describe('NgDropImagesDirective', () => {
  it('should create an instance', () => {
    const directive = new NgDropImagesDirective();
    expect(directive).toBeTruthy();
  });
});
